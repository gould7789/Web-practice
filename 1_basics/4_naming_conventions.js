/**
 * Naming Conventions
 * 
 * 1) 일반적으로 영어를 사용하며 문자와 숫자를 사용 가능
 * 2) 특수기호는 언더스코어와 달러를 사용 할 수 있음
 * 3) 숫자로 이름을 시작할 수 없음
 * 4) 키워드는 변수명으로 사용 할 수 없음
 */

let code = '코드';
var $code = '코드';
const _code = '코드';

console.log(code);
console.log($code);
console.log(_code);

/**
 * Naming Conventions 2
 * 
 * 1) camelCase - 대부분의 언어에서 많이 사용
 * 2) snake_case - 파이썬 같은 언어에서 사용
 * 3) PascalCase - C# 마이크로소프트 계열의 언어에서 사용
 */